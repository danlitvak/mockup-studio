node_modules/
dist/
dist-ssr/
coverage/
*.local
.DS_Store
.vite/
test-results/
playwright-report/
blob-report/
.playwright/
*.tsbuildinfo

# Compiled output of the TypeScript configs. These must never be committed:
# Vite resolves vite.config.js *before* vite.config.ts, so a stale copy
# silently shadows the real config and edits to the .ts appear to do nothing.
/vite.config.js
/playwright.config.js

